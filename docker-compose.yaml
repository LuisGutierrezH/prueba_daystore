version: '3.7'
services:
  odoo_alejandria:
    container_name: odoo_alejandria
    image: odoo:17.0
    restart: always
    depends_on:
      - db_alejandria
    ports:
      - 8069:8069
    volumes:
      - ./volumes/odoo-data:/var/lib/odoo
      - ./odoo/config/local.conf:/etc/odoo/odoo.conf
      - ./extra-addons:/mnt/extra-addons
  db_alejandria:
    container_name: db_alejandria
    image: postgres:15
    restart: always
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_PASSWORD=odoo
      - POSTGRES_USER=odoo
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      -  ${PWD}/volumes/postgres-data:/var/lib/postgresql/data/pgdata
    ports:
      - 5432:5432
